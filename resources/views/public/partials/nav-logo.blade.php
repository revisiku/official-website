<a href="{{ LangService::url('/') }}" class="logo">
    <img id="nav-logo" src="{{ asset($favicon) }}">
    <span>{{ $site_name }}</span> <span class="separator">|</span> <span class="word"></span>
</a>
